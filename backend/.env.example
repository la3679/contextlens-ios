# Backend only. Keep real credentials in the ignored backend/.env.
OPENAI_API_KEY=
# Optional override. Missing or blank uses this pinned default.
OPENAI_MODEL=gpt-5.4-mini-2026-03-17
APP_ENV=development
