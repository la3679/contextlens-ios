# Backend only. Keep real credentials in the ignored backend/.env.
OPENAI_API_KEY=
# Model selection will be validated against official documentation at integration.
OPENAI_MODEL=
APP_ENV=development
